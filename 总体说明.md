# 项目需求分析与后续开发规划

## 一、需求本质分析

你的需求本质是**坐标转换问题**。核心是：
- 多个坐标系之间存在刚性变换（旋转+平移）
- 通过已知的对应点对，求解坐标系之间的转换关系
- 然后将任意点从源坐标系转换到目标坐标系

## 二、数据模型设计

### 1. 坐标系类 (CoordinateSystem)
```
- id: 唯一标识
- name: 名称（如"全站仪坐标系A"）
- origin: 原点位置 (x, y, z)
- rotation: 旋转矩阵 (3x3) 或欧拉角
- basisVectors: 三个坐标轴的方向向量
```

### 2. 点类 (Point)
```
- id: 唯一标识
- coordinates: 在当前坐标系下的坐标 (x, y, z)
- groupName: 所属分组
- color: 显示颜色
- correspondences: 与其他坐标系点的对应关系列表
```

### 3. 点对应关系 (PointCorrespondence)
```
- sourceSystemId: 源坐标系ID
- sourcePointId: 源点ID
- targetSystemId: 目标坐标系ID
- targetPointId: 目标点ID
```

## 三、核心算法：坐标转换

### 问题类型
1. **已知对应点对 → 求转换矩阵**（刚体变换）
2. **已知转换矩阵 + 源点 → 求目标点坐标**（正变换）

### 算法选择

**方法一：Kabsch算法（推荐）**
- 输入：n ≥ 3 个对应点对（源坐标系和目标坐标系中的坐标）
- 输出：旋转矩阵 R 和平移向量 t
- 原理：最小二乘意义上的最优旋转

**方法二：SVD方法**
- 类似Kabsch，使用奇异值分解

**方法三：已知两个坐标系原点+一个轴方向**
- 如果知道坐标系的原点和一个轴的方向向量，可以直接计算

### 转换公式
```
P_target = R * P_source + t
```

其中：
- R = 3x3 旋转矩阵
- t = 3x1 平移向量

## 四、功能模块规划

### 模块1：坐标系管理
1. **创建坐标系**
   - 手动输入：原点位置 + 三个轴的方向向量
   - 或通过对应点对自动计算

2. **编辑坐标系**
   - 修改名称、颜色等属性
   - 调整原点或轴方向

3. **删除坐标系**

4. **坐标系列表**
   - 显示所有坐标系
   - 切换激活坐标系

### 模块2：点管理
1. **在不同坐标系下添加点**
2. **点的分组管理**
3. **点列表显示**
4. **选择/高亮对应点**

### 模块3：对应关系定义
1. **建立点对应关系**
   - 选择源坐标系 + 源点
   - 选择目标坐标系 + 对应点
   - 标记为"已配对"

2. **查看/编辑对应关系**
   - 以表格形式显示所有配对点
   - 可删除错误配对

3. **配对验证**
   - 检查配对是否一致
   - 计算配对误差

### 模块4：坐标转换计算
1. **计算转换关系**
   - 选择源坐标系和目标坐标系
   - 使用已建立的对应点对
   - 计算R和t矩阵
   - 显示转换误差（均方根误差RMSE）

2. **执行坐标转换**
   - 选择源坐标系 + 源点
   - 选择目标坐标系
   - 显示转换后的坐标
   - 可选：将转换后的点添加到目标坐标系

3. **批量转换**
   - 选择多个点或一个分组
   - 一次性转换

### 模块5：可视化
1. **多坐标系同时显示**
   - 用不同颜色区分坐标系
   - 显示坐标系原点和平面

2. **对应点连线**
   - 可视化配对关系
   - 用线条连接对应的点

3. **转换结果预览**
   - 显示转换前后的点位置

## 五、UI界面设计建议

### 方案A：多面板布局
```
+------------------+------------------+
|    坐标系列表    |                  |
|    +--------+    |                  |
|    | 坐标系A|    |                  |
|    | 坐标系B|    |    VTK 3D视图    |
|    | 坐标系C|    |                  |
|    +--------+    |                  |
|------------------|                  |
|    点列表        |                  |
|    (当前坐标系)  |                  |
+------------------+------------------+
|          转换控制面板                |
|  源: [坐标系A] 点:[1,2,3]            |
|  目标: [坐标系B]                     |
|  [计算转换] [执行转换]                |
|  结果: (1.2, 3.4, 5.6)              |
+------------------------------------+
```

### 方案B：标签页式
- 标签页1：数据管理（坐标系+点）
- 标签页2：配对关系
- 标签页3：坐标转换
- 标签页4：3D可视化设置

## 六、实现步骤

### 第一阶段：基础数据模型
1. 创建CoordinateSystem类
2. 创建PointCorrrespondence类
3. 修改PointData结构支持多坐标系
4. 重构数据存储结构

### 第二阶段：坐标系管理UI
1. 添加坐标系管理面板
2. 实现坐标系的CRUD操作
3. 在VTK中可视化坐标系（原点+轴）

### 第三阶段：对应关系管理
1. 实现点配对UI
2. 建立/删除对应关系
3. 显示配对线条

### 第四阶段：转换算法
1. 实现Kabsch算法
2. 计算转换矩阵R和t
3. 计算并显示转换误差

### 第五阶段：转换功能
1. 单点转换
2. 批量转换
3. 转换结果查看

### 第六阶段：数据持久化
1. 设计文件格式（JSON或XML）
2. 保存/加载项目
3. 导出转换结果

## 七、技术要点

### 1. 转换矩阵的表示
- 旋转矩阵（3x3）- 优点：直接乘法
- 四元数 - 优点：避免万向锁
- 欧拉角 - 优点：直观

### 2. 误差评估
```cpp
RMSE = sqrt(sum(|P_transformed - P_target|^2) / n)
```

### 3. 奇异值分解（SVD）
使用Eigen库的SVD来求解最优旋转矩阵

### 4. 可视化技巧
- 每个坐标系用不同颜色的轴表示
- 原点用小球标记
- 对应点之间用虚线连接

## 八、潜在难点

1. **至少需要3对对应点**才能唯一确定转换关系
2. **配对错误检测**：需要识别并处理错误配对
3. **多坐标系链式转换**：A→B→C的转换
4. **数值稳定性**：SVD求解时的奇异性问题

## 九、建议的代码结构

```
VTK930/
├── CoordinateSystem.h/cpp        # 坐标系类
├── PointCorrespondence.h/cpp     # 点对应关系
├── CoordinateTransformer.h/cpp   # 坐标转换算法
├── CoordinateManagerDialog.h/cpp # 坐标系管理对话框
├── CorrespondenceDialog.h/cpp   # 配对管理对话框
├── TransformDialog.h/cpp         # 转换计算对话框
├── DataManager.h/cpp             # 数据管理器（单例）
└── VTK930.h/cpp                  # 主窗口
```

---

**需要我先实现哪个模块？建议从第一阶段开始，先建立数据模型和坐标系管理功能。**