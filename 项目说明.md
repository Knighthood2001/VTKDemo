# VTK930 项目说明文档

## 一、项目概述

VTK930 是一个基于 Qt + PCL + VTK 的点云坐标转换工具。主要功能包括：
- 点云数据加载与管理
- 多分组点云显示
- 点配对与坐标转换矩阵计算
- 批量坐标转换

## 二、文件结构

```
VTK930/
├── VTK930.h/cpp              # 主窗口类（核心逻辑）
├── VTK930.ui                 # 主窗口UI定义
├── PointCloudInputDialog.h/cpp   # 点云输入对话框
├── PointPairDialog.h/cpp     # 点配对对话框
├── TransformResultDialog.h/cpp    # 转换结果对话框
├── GroupNameDialog.h/cpp     # 分组名称输入对话框
└── 总体说明.md               # 需求分析与开发规划
```

## 三、核心类与数据结构

### 1. PointData（点数据）

```cpp
struct PointData {
    std::string id;        // 点ID
    float x, y, z;        // 坐标
    std::string groupName; // 所属分组
    int colorR, colorG, colorB; // 显示颜色
};
```

### 2. Point3D（三维点）

```cpp
struct Point3D {
    float x, y, z;
    std::string id;
    std::string groupName;
};
```

### 3. PointPair（点配对）

```cpp
struct PointPair {
    std::string sourceGroup;    // 源分组
    std::string targetGroup;    // 目标分组
    std::string sourceId;       // 源点ID
    std::string targetId;       // 目标点ID
    Point3D sourcePoint;       // 源点坐标
    Point3D targetPoint;       // 目标点坐标
};
```

### 4. TransformationMatrix（转换矩阵）

```cpp
struct TransformationMatrix {
    Eigen::Matrix3d rotation;     // 3x3旋转矩阵
    Eigen::Vector3d translation;  // 3x1平移向量
    
    // 转换为4x4齐次矩阵
    Eigen::Matrix4d toMatrix() const;
};
```

## 四、VTK930 主窗口类

### 成员变量

| 变量名 | 类型 | 说明 |
|--------|------|------|
| `ui` | Ui::VTK930 | Qt UI对象 |
| `cloud` | pcl::PointCloud::Ptr | 原始点云（单PCD加载） |
| `view` | PCLVisualizer::Ptr | 3D可视化器 |
| `groupClouds` | map<string, Ptr> | 分组点云集合 |
| `allPoints` | vector<PointData> | 所有点数据 |
| `pointPairs` | vector<PointPair> | 点配对列表 |
| `pointPicker` | vtkPointPicker* | VTK点选器 |
| `pointSize` | int | 点云点大小 |

### 核心函数

#### 初始化
- `initialVtkWidget()` - 初始化VTK 3D视图
- `initOrientationMarker()` - 初始化坐标系显示
- `setupPointPicking()` - 设置点选功能

#### 点云管理
- `onOpen()` - 加载PCD文件
- `onLoad()` - 打开点云输入对话框
- `addPointsToVisualizer()` - 添加点到可视化器
- `updatePointTable()` - 更新点列表
- `updateGroupList()` - 更新分组列表
- `updateFilterCombo()` - 更新过滤器

#### 配对管理
- `onAddPair()` - 添加配对
- `onDeletePair()` - 删除配对
- `updatePairList()` - 更新配对列表
- `getAvailablePairs()` - 获取可用配对组

#### 转换计算
- `computeTransformAB()` - 计算A→B转换矩阵
- `computeTransformBA()` - 计算B→A转换矩阵
- `computeTransformBetween()` - 计算指定组间转换矩阵
- `onViewTransform()` - 查看转换结果
- `onRecomputeTransform()` - 重新计算转换矩阵
- `onBatchTransform()` - 批量转换点云

#### 设置
- `onSetPointSize()` - 设置点云点大小

## 五、对话框类

### 1. PointCloudInputDialog
- 手动输入点坐标
- 指定所属分组
- 设置点颜色

### 2. PointPairDialog
- 选择源分组和目标分组
- 从列表中选择对应点
- 添加/删除配对

### 3. TransformResultDialog
- 显示转换矩阵（A→B 和 B→A）
- 矩阵复制功能
- 坐标转换计算器

### 4. GroupNameDialog
- 输入新分组名称

## 六、菜单栏

| 菜单 | 功能 |
|------|------|
| 文件 | 打开PCD文件、加载点 |
| 设置 | 点云点大小 |

## 七、核心算法

### 转换矩阵计算（Umeyama算法）

使用Eigen库的SVD求解最优刚性变换矩阵：

1. 计算两组点的质心
2. 去中心化
3. 计算协方差矩阵
4. SVD分解
5. 求解旋转矩阵R和平移向量t

```cpp
TransformationMatrix VTK930::computeTransformAB() {
    // 提取配对点的源坐标和目标坐标
    // 使用Eigen::JacobiSVD计算最优变换
    // 返回旋转矩阵+平移向量
}
```

## 八、VTK可视化

### 点云渲染
- 使用PCLVisualizer渲染点云
- 每组点云独立颜色
- 支持点大小设置

### 坐标系显示
- 使用vtkAxesActor显示三维坐标系
- vtkOrientationMarkerWidget可交互显示

### 点选功能
- vtkPointPicker实现鼠标拾取
- 左键点击选择点
- 回调函数显示点坐标

## 九、依赖库

| 库 | 版本 | 用途 |
|----|------|------|
| Qt | 5.15.2 | GUI框架 |
| PCL | 1.14.0 | 点云处理 |
| VTK | 9.3 | 3D可视化 |
| Eigen | 3.4.0 | 矩阵运算 |

## 十、后续开发建议

参考 `总体说明.md`，建议按以下阶段开发：

1. **数据模型重构** - 引入CoordinateSystem类
2. **坐标系管理** - 坐标系CRUD和可视化
3. **配对增强** - 配对验证和误差显示
4. **数据持久化** - JSON/XML保存项目

---

*文档更新时间：2026-04-08*
